name: Hello World
on:
  push:

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Hello World"
      - name: API_KEY
        run: echo "Usando a API_KEY ${{ secrets.API_KEY}}"
